title: "Análisis de canciones de kid voodoo"
author: "Alanis Romero"
date: "2025-10-21"
output: html_document
---

#Instalamos y cargamos librerías necesarios

install.packages("spotifyr")
library(spotifyr)
install.packages('dplyr')
install.packages('ggplot2')

#Conexión co el API de spotify: Antes de comenzar, necesitamos generar un token de acceso con nuestras credenciales de Spotify Developer.

Sys.setenv(SPOTIFY_CLIENT_ID = '6927007fbc2c4447b7c46c3b9396fb07')
Sys.setenv(SPOTIFY_CLIENT_SECRET = '0114ac183ba2435386492c17b2ed0163')

access_token <- get_spotify_access_token()

access_token

#buscamos al artista

kidpudu <- search_spotify("Kid Voodoo", type = "artist")
kidpudu$id

#Reemplazá el ID con el correcto que te devuelva el código anterior
artist_id <- "4wQUr0S2t3Ub4khxR5EPSc"
#Obtenemos las canciones del artista más populares
top_tracks <- get_artist_top_tracks(artist_id, market = "US")
head(top_tracks)

install.packages("remotes")
remotes::install_github("charlie86/spotifyr")
library(spotifyr)


